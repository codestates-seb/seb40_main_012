spring:
  h2:
    console:
      enabled: true
      path: /h2  # Context path
  datasource:
    url: jdbc:h2:mem:CherryPick  # JDBC URL
  jpa:
    hibernate:
      ddl-auto: create  # ??? ?? ??
    show-sql: true  # SQL ?? ??
servlet:
  multipart:
    location: /Users/gnidinger/Desktop
    maxFileSize: 5MB
    maxRequestSize: 10MB